# Descrição

5- Implemente o seguinte programa em Java:

- Crie uma classe Estudante com as seguintes características:

  ->Atributos encapsulados: nome, nota final (de 0 a 100).
  ->Métodos get()/set()
  ->Método toString() para exibir os dados de um estudante.

- Crie uma classe Principal com as seguintes características:

  -> Crie um vetor para armazenar os dados de cinco estudantes (os dados devem
  ser fornecidos pelo usuário).

  -> Ao final da inserção dos cinco estudantes, calcule e exiba:

  1. A média da turma,
  2. A quantidade de estudantes que foram para o exame final (40 <=
     média <= 69)
  3. A quantidade de estudantes que foram reprovados (média < 40),
  4. Os dados dos estudantes aprovados (média >= 70).
