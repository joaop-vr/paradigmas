# Descrição

3- Implemente uma classe Data com os atributos dia, mês e ano encapsulados e os
métodos:

    - get()/set(): para cada atributo da classe

    - ajustarData(): recebe dia, mês e ano de nascimento como parâmetros e preenche
    os atributos da classe correspondente.

    - imprimirData(): exibe a data de nascimento no formato dia/mês/ano.
